## Summary

<!-- What does this change, and why? -->

## Checklist

- [ ] `npm run verify` passes
- [ ] Conventional commit (`feat:`, `fix:`, `test:`, `chore:`, `docs:`)
- [ ] No `any` in `src/`
- [ ] Output stays deterministic (no timestamps, no absolute paths)
- [ ] `sync` does not modify `AGENTS.md`
